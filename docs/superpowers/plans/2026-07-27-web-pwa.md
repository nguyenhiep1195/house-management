# Web PWA Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Biến `apps/web` (Next.js 16) thành PWA cài đặt được trên mobile, có offline shell nhẹ và thanh cảnh báo offline, không cache dữ liệu riêng tư.

**Architecture:** Dùng Serwist (`@serwist/next`) sinh service worker precache tài sản build tĩnh + runtime cache mặc định; điều hướng offline chưa cache rơi vào trang `/~offline`. Manifest dùng file native `app/manifest.ts`. Một client component lắng nghe sự kiện `online`/`offline` để hiện thanh đỏ ở header. KHÔNG cache HTML/RSC trang đăng nhập.

**Tech Stack:** Next.js 16.2 (App Router, RSC), React 19, Serwist, Tailwind v4, shadcn/ui, pnpm.

## Global Constraints

- Package manager: **pnpm**, chạy lệnh trong `apps/web`.
- Copy hướng tới người dùng: **tiếng Việt**; code/comment/identifier: tiếng Anh.
- Next.js 16 build mặc định Turbopack, nhưng Serwist là webpack plugin → **build phải dùng `next build --webpack`**; `dev` giữ Turbopack.
- Service worker **tắt ở dev** (`disable: process.env.NODE_ENV === "development"`).
- KHÔNG đưa HTML/RSC trang đăng nhập vào Cache Storage — chỉ precache tài sản build tĩnh.
- App web thuộc **root repo** `house-management` (không có nested `.git`); commit từ root repo. Không có test runner trong `apps/web` — dùng `pnpm lint` / `pnpm build` / kiểm tra DevTools làm gate.
- Đang làm trên nhánh `feat/web-pwa`.
- File icon/manifest path: icon ở `public/icons/`, manifest phục vụ tự động tại `/manifest.webmanifest`.
- `theme_color`: `#0a0a0a` (khớp `--primary` gần đen của theme sáng); `background_color`: `#ffffff`.

---

### Task 1: Dependencies, build script, gitignore

**Files:**
- Modify: `apps/web/package.json` (scripts + dependencies)
- Modify: `apps/web/.gitignore`

**Interfaces:**
- Consumes: nothing.
- Produces: gói `@serwist/next` + `serwist` sẵn sàng import; script `build` dùng `--webpack`; `public/sw.js` được git bỏ qua.

- [ ] **Step 1: Cài dependencies**

```bash
cd apps/web
pnpm add @serwist/next serwist
```

- [ ] **Step 2: Đổi build script sang webpack**

Trong `apps/web/package.json`, đổi `"build": "next build"` thành:

```json
    "build": "next build --webpack",
```

Giữ nguyên `"dev": "next dev"` và `"start": "next start"`.

- [ ] **Step 3: Bỏ qua service worker sinh tự động**

Thêm vào cuối `apps/web/.gitignore`:

```
# serwist (generated service worker)
public/sw.js
public/sw.js.map
public/swe-worker-*.js
```

- [ ] **Step 4: Kiểm tra install sạch**

Run: `cd apps/web && pnpm install`
Expected: không lỗi; `node_modules/@serwist/next` và `node_modules/serwist` tồn tại.

- [ ] **Step 5: Commit**

```bash
cd /Users/hiepnn/projects/house-management
git add apps/web/package.json apps/web/pnpm-lock.yaml apps/web/.gitignore
git commit -m "chore(web): add serwist deps and webpack build for PWA"
```

---

### Task 2: Trang offline fallback

**Files:**
- Create: `apps/web/app/~offline/page.tsx`

**Interfaces:**
- Consumes: nothing.
- Produces: route `/~offline` render tĩnh, dùng làm fallback điều hướng cho service worker (Task 3).

- [ ] **Step 1: Tạo trang offline**

Tạo `apps/web/app/~offline/page.tsx`:

```tsx
import type { Metadata } from "next";
import { WifiOff } from "lucide-react";

export const metadata: Metadata = {
  title: "Ngoại tuyến",
};

export default function OfflinePage() {
  return (
    <main className="flex min-h-dvh flex-col items-center justify-center gap-4 p-6 text-center">
      <WifiOff className="size-12 text-muted-foreground" aria-hidden />
      <h1 className="text-xl font-semibold">Bạn đang ngoại tuyến</h1>
      <p className="max-w-sm text-sm text-muted-foreground">
        Không có kết nối mạng nên không thể tải trang này. Vui lòng kiểm tra kết
        nối và thử lại.
      </p>
    </main>
  );
}
```

Ghi chú: không thêm nút "Thử lại" dạng client — trang này phải render tĩnh để
precache được. Người dùng tải lại bằng trình duyệt.

- [ ] **Step 2: Kiểm tra route dev**

Run: `cd apps/web && pnpm dev` rồi mở `http://localhost:3000/~offline`
Expected: hiện icon + "Bạn đang ngoại tuyến". Dừng dev server.

- [ ] **Step 3: Lint**

Run: `cd apps/web && pnpm lint`
Expected: không lỗi mới.

- [ ] **Step 4: Commit**

```bash
cd /Users/hiepnn/projects/house-management
git add apps/web/app/~offline/page.tsx
git commit -m "feat(web): add offline fallback page"
```

---

### Task 3: Service worker + cấu hình Serwist

**Files:**
- Create: `apps/web/app/sw.ts`
- Modify: `apps/web/next.config.ts`

**Interfaces:**
- Consumes: route `/~offline` (Task 2).
- Produces: `public/sw.js` sinh khi build; SW tự đăng ký (mặc định `register: true` của `@serwist/next`).

- [ ] **Step 1: Tạo service worker nguồn**

Tạo `apps/web/app/sw.ts`:

```ts
import { defaultCache } from "@serwist/next/worker";
import type { PrecacheEntry, SerwistGlobalConfig } from "serwist";
import { Serwist } from "serwist";

declare global {
  interface WorkerGlobalScope extends SerwistGlobalConfig {
    __SW_MANIFEST: (PrecacheEntry | string)[] | undefined;
  }
}

declare const self: WorkerGlobalScope;

const serwist = new Serwist({
  precacheEntries: self.__SW_MANIFEST,
  skipWaiting: true,
  clientsClaim: true,
  navigationPreload: true,
  runtimeCaching: defaultCache,
  fallbacks: {
    entries: [
      {
        url: "/~offline",
        matcher: ({ request }) => request.destination === "document",
      },
    ],
  },
});

serwist.addEventListeners();
```

- [ ] **Step 2: Bọc next.config bằng withSerwist**

Sửa `apps/web/next.config.ts` thành:

```ts
import type { NextConfig } from "next";
import withSerwistInit from "@serwist/next";

const withSerwist = withSerwistInit({
  swSrc: "app/sw.ts",
  swDest: "public/sw.js",
  disable: process.env.NODE_ENV === "development",
});

const nextConfig: NextConfig = {};

export default withSerwist(nextConfig);
```

- [ ] **Step 3: Build sinh service worker**

Run: `cd apps/web && pnpm build`
Expected: build thành công; file `apps/web/public/sw.js` được sinh ra.

- [ ] **Step 4: Kiểm tra SW đăng ký & offline fallback**

Run: `cd apps/web && pnpm start`, mở `http://localhost:3000`, DevTools → Application:
- Service Workers: có một SW `activated and running`.
- Bật "Offline", điều hướng tới một route chưa từng mở (vd `/rooms`) → hiện trang `/~offline`.

Dừng server.

- [ ] **Step 5: Commit**

```bash
cd /Users/hiepnn/projects/house-management
git add apps/web/app/sw.ts apps/web/next.config.ts
git commit -m "feat(web): add serwist service worker with offline fallback"
```

---

### Task 4: Icon PWA tạm

**Files:**
- Create: `apps/web/public/icons/icon-192.png`
- Create: `apps/web/public/icons/icon-512.png`
- Create: `apps/web/public/icons/maskable-512.png`
- Create: `apps/web/public/icons/apple-touch-icon.png`
- Create: `apps/web/scripts/generate-icons.mjs` (script sinh icon, giữ lại để tái tạo)

**Interfaces:**
- Consumes: nothing.
- Produces: 4 file PNG icon dùng bởi manifest (Task 5) và apple-touch (Task 5).

- [ ] **Step 1: Viết script sinh icon bằng sharp**

Cài công cụ dev tạm: `cd apps/web && pnpm add -D sharp`

Tạo `apps/web/scripts/generate-icons.mjs`:

```js
import { mkdir, writeFile } from "node:fs/promises";
import { fileURLToPath } from "node:url";
import { dirname, join } from "node:path";
import sharp from "sharp";

const OUT = join(dirname(fileURLToPath(import.meta.url)), "..", "public", "icons");
const BG = "#0a0a0a";
const FG = "#ffffff";

// SVG chữ "HM" căn giữa; padPct = viền an toàn cho maskable
function svg(size, padPct) {
  const fontSize = Math.round(size * (padPct ? 0.32 : 0.42));
  return `<svg xmlns="http://www.w3.org/2000/svg" width="${size}" height="${size}">
    <rect width="${size}" height="${size}" rx="${padPct ? 0 : Math.round(size * 0.18)}" fill="${BG}"/>
    <text x="50%" y="50%" dy="0.35em" text-anchor="middle"
      font-family="Arial, Helvetica, sans-serif" font-weight="700"
      font-size="${fontSize}" fill="${FG}">HM</text>
  </svg>`;
}

async function png(size, name, padPct = 0) {
  await sharp(Buffer.from(svg(size, padPct))).png().toFile(join(OUT, name));
  console.log("wrote", name);
}

await mkdir(OUT, { recursive: true });
await png(192, "icon-192.png");
await png(512, "icon-512.png");
await png(512, "maskable-512.png", 0.1); // nền phủ toàn khung, chữ nhỏ hơn
await png(180, "apple-touch-icon.png");
```

- [ ] **Step 2: Chạy script sinh icon**

Run: `cd apps/web && node scripts/generate-icons.mjs`
Expected: in ra 4 dòng "wrote ..."; 4 file PNG xuất hiện trong `apps/web/public/icons/`.

- [ ] **Step 3: Gỡ sharp khỏi dependencies**

Sharp chỉ cần để sinh icon một lần, không cần trong runtime:

Run: `cd apps/web && pnpm remove sharp`
Expected: `sharp` biến khỏi `devDependencies`; các file PNG đã sinh vẫn còn.

- [ ] **Step 4: Kiểm tra file**

Run: `cd apps/web && ls -la public/icons`
Expected: đủ `icon-192.png`, `icon-512.png`, `maskable-512.png`, `apple-touch-icon.png` (kích thước > 0).

- [ ] **Step 5: Commit**

```bash
cd /Users/hiepnn/projects/house-management
git add apps/web/public/icons apps/web/scripts/generate-icons.mjs apps/web/package.json apps/web/pnpm-lock.yaml
git commit -m "feat(web): add placeholder PWA icons and generator script"
```

---

### Task 5: Manifest + metadata layout

**Files:**
- Create: `apps/web/app/manifest.ts`
- Modify: `apps/web/app/layout.tsx`

**Interfaces:**
- Consumes: icon từ Task 4 (`/icons/*.png`).
- Produces: `/manifest.webmanifest` hợp lệ; `<head>` có `theme-color`, apple-touch-icon, apple-web-app metadata.

- [ ] **Step 1: Tạo manifest**

Tạo `apps/web/app/manifest.ts`:

```ts
import type { MetadataRoute } from "next";

export default function manifest(): MetadataRoute.Manifest {
  return {
    name: "House Management",
    short_name: "House Mgmt",
    description: "Hệ thống quản trị nhà ở",
    lang: "vi",
    start_url: "/",
    display: "standalone",
    background_color: "#ffffff",
    theme_color: "#0a0a0a",
    icons: [
      { src: "/icons/icon-192.png", sizes: "192x192", type: "image/png" },
      { src: "/icons/icon-512.png", sizes: "512x512", type: "image/png" },
      {
        src: "/icons/maskable-512.png",
        sizes: "512x512",
        type: "image/png",
        purpose: "maskable",
      },
    ],
  };
}
```

- [ ] **Step 2: Thêm viewport + apple metadata vào layout**

Trong `apps/web/app/layout.tsx`:

Thêm `Viewport` vào import type:

```ts
import type { Metadata, Viewport } from "next";
```

Mở rộng `metadata` (thêm hai field vào object hiện có):

```ts
export const metadata: Metadata = {
  title: {
    default: "House Management",
    template: "%s | House Management",
  },
  description: "Hệ thống quản trị nhà ở",
  appleWebApp: {
    capable: true,
    statusBarStyle: "default",
    title: "House Mgmt",
  },
  icons: {
    apple: "/icons/apple-touch-icon.png",
  },
};
```

Thêm export `viewport` ngay sau `metadata`:

```ts
export const viewport: Viewport = {
  themeColor: [
    { media: "(prefers-color-scheme: light)", color: "#ffffff" },
    { media: "(prefers-color-scheme: dark)", color: "#0a0a0a" },
  ],
};
```

- [ ] **Step 3: Build và kiểm tra manifest**

Run: `cd apps/web && pnpm build && pnpm start`
Mở `http://localhost:3000/manifest.webmanifest`
Expected: JSON manifest đúng nội dung trên. DevTools → Application → Manifest: không lỗi, hiện đủ icon, "Installable".

- [ ] **Step 4: Lint**

Run: `cd apps/web && pnpm lint`
Expected: không lỗi mới.

- [ ] **Step 5: Commit**

```bash
cd /Users/hiepnn/projects/house-management
git add apps/web/app/manifest.ts apps/web/app/layout.tsx
git commit -m "feat(web): add web app manifest and apple PWA metadata"
```

---

### Task 6: Thanh cảnh báo offline ở header

**Files:**
- Create: `apps/web/components/shared/offline-indicator.tsx`
- Modify: `apps/web/components/layout/site-header.tsx`

**Interfaces:**
- Consumes: nothing (dùng `navigator.onLine` + window events).
- Produces: component `<OfflineIndicator />` render thanh đỏ khi offline, `null` khi online.

- [ ] **Step 1: Tạo component OfflineIndicator**

Tạo `apps/web/components/shared/offline-indicator.tsx`:

```tsx
"use client";

import { useEffect, useState } from "react";
import { WifiOff } from "lucide-react";

export function OfflineIndicator() {
  const [isOffline, setIsOffline] = useState(false);

  useEffect(() => {
    const update = () => setIsOffline(!navigator.onLine);
    update();
    window.addEventListener("online", update);
    window.addEventListener("offline", update);
    return () => {
      window.removeEventListener("online", update);
      window.removeEventListener("offline", update);
    };
  }, []);

  if (!isOffline) return null;

  return (
    <div
      role="status"
      className="flex items-center justify-center gap-2 bg-destructive px-4 py-1 text-center text-xs font-medium text-white"
    >
      <WifiOff className="size-3.5" aria-hidden />
      Mất kết nối mạng — một số chức năng có thể không hoạt động
    </div>
  );
}
```

Ghi chú: khởi tạo state `false` rồi cập nhật trong `useEffect` để tránh lệch
hydration (server không biết trạng thái mạng).

- [ ] **Step 2: Nhúng vào site-header**

Trong `apps/web/components/layout/site-header.tsx`, thêm import:

```tsx
import { OfflineIndicator } from "@/components/shared/offline-indicator";
```

Bọc phần trả về để thanh nằm phía trên `<header>` (thanh chiếm nguyên chiều
ngang, không nằm trong flex của header). Thay `return (` hiện tại bằng cách bọc
trong fragment:

```tsx
  return (
    <>
      <OfflineIndicator />
      <header className="sticky top-0 z-10 flex h-14 shrink-0 items-center gap-2 border-b bg-background/95 px-4 backdrop-blur supports-[backdrop-filter]:bg-background/80">
        {/* ...nội dung header giữ nguyên... */}
      </header>
    </>
  );
```

(Đóng `</>` sau `</header>`.)

- [ ] **Step 3: Kiểm tra thủ công**

Run: `cd apps/web && pnpm dev`, đăng nhập vào một trang admin. DevTools → Network → "Offline":
Expected: thanh đỏ "Mất kết nối mạng..." hiện phía trên header. Tắt "Offline" → thanh biến mất. Dừng server.

- [ ] **Step 4: Lint**

Run: `cd apps/web && pnpm lint`
Expected: không lỗi mới.

- [ ] **Step 5: Commit**

```bash
cd /Users/hiepnn/projects/house-management
git add apps/web/components/shared/offline-indicator.tsx apps/web/components/layout/site-header.tsx
git commit -m "feat(web): show offline warning banner in header"
```

---

### Task 7: Rà soát responsive nhanh

**Files:**
- Modify: (chỉ khi cần) các trang bảng trong `apps/web/features/*/components/*.tsx` và `apps/web/app/(admin)/**`

**Interfaces:**
- Consumes: nothing.
- Produces: các bảng không tràn vỡ layout ở viewport 375px.

Bối cảnh: shadcn `Table` (`components/ui/table.tsx`) đã bọc sẵn
`overflow-x-auto`, nên bảng dùng `<Table>` đã cuộn ngang được. Task này là
KIỂM TRA + chỉ vá chỗ thật sự vỡ.

- [ ] **Step 1: Xác nhận các bảng dùng shadcn Table**

Run:
```bash
cd apps/web && grep -rL "@/components/ui/table" \
  features/contracts/components/contracts-table.tsx \
  features/invoices/components/invoice-list.tsx \
  features/rooms/components/rooms-table.tsx \
  features/tenants/components/tenants-table.tsx \
  features/users/components/users-table.tsx
```
Expected: không in ra file nào (mọi bảng đều import shadcn Table). Nếu có file in ra, mở file đó và bọc bảng thô trong `<div className="w-full overflow-x-auto">`.

- [ ] **Step 2: Kiểm tra thị giác ở 375px**

Run: `cd apps/web && pnpm dev`, mở DevTools device toolbar ở 375×812, đăng nhập và duyệt lần lượt: `/`, `/rooms`, `/contracts`, `/invoices`, `/tenants`, `/users`, `/settings`.
Expected: không có phần tử nào đẩy body tràn ngang (không có thanh cuộn ngang toàn trang). Bảng dài cuộn được trong khung của nó. Sidebar mở dạng Sheet.

- [ ] **Step 3: Vá chỗ vỡ (chỉ khi Step 2 phát hiện)**

Với mỗi chỗ tràn thực sự: bọc phần tử gây tràn trong `<div className="w-full overflow-x-auto">`, hoặc thêm `min-w-0`/`flex-wrap` cho hàng flex bị tràn. KHÔNG refactor bảng thành card, KHÔNG đổi bố cục lớn. Nếu Step 2 sạch, bỏ qua step này.

- [ ] **Step 4: Lint (nếu có sửa file)**

Run: `cd apps/web && pnpm lint`
Expected: không lỗi mới.

- [ ] **Step 5: Commit (nếu có sửa file)**

```bash
cd /Users/hiepnn/projects/house-management
git add -A apps/web
git commit -m "fix(web): keep data tables from overflowing on mobile"
```

Nếu không sửa gì (Step 2 sạch), ghi chú "no changes needed" và bỏ qua commit.

---

### Task 8: Nghiệm thu PWA end-to-end

**Files:** none (verification only).

**Interfaces:**
- Consumes: toàn bộ thay đổi Task 1–7.
- Produces: xác nhận PWA đạt yêu cầu nghiệm thu trong spec §6.

- [ ] **Step 1: Build production sạch**

Run: `cd apps/web && pnpm build`
Expected: build thành công với `--webpack`; sinh `public/sw.js`; không cảnh báo về Serwist.

- [ ] **Step 2: Chạy production và kiểm PWA**

Run: `cd apps/web && pnpm start`. DevTools → Application:
- Manifest: hợp lệ, đủ 3 icon, "Installable".
- Service Workers: 1 SW `activated`.
- Lighthouse (mobile): mục "Installable" pass.

- [ ] **Step 3: Kiểm offline shell + banner**

Với server đang chạy: mở app, DevTools "Offline":
- Điều hướng tới route chưa cache → trang `/~offline`.
- Thanh đỏ offline hiện ở header; bỏ "Offline" → biến mất.

- [ ] **Step 4: Kiểm không rò rỉ dữ liệu riêng tư**

DevTools → Application → Cache Storage: xác nhận KHÔNG có mục nào lưu HTML/RSC của trang đăng nhập (chỉ có `_next/static`, ảnh, font, `/~offline`). Dừng server.

- [ ] **Step 5: Đẩy nhánh (nếu người dùng yêu cầu tạo PR)**

Chỉ khi người dùng yêu cầu:

```bash
cd /Users/hiepnn/projects/house-management
git push -u origin feat/web-pwa
```

---

## Self-Review

**Spec coverage:**
- §1 quyết định (Serwist / installable+shell / icon tạm / responsive nhanh / không cache trang / banner đỏ) → Task 1–8 phủ đủ; banner đỏ = Task 6.
- §2 ràng buộc Turbopack/`--webpack` → Task 1 Step 2.
- §3 các file → Task 2–6 tạo đủ (next.config, sw.ts, manifest.ts, ~offline, icons, offline-indicator, site-header, layout, package.json, .gitignore).
- §4 chi tiết triển khai → khớp code trong Task 2/3/5/6; icon Task 4.
- §5 responsive → Task 7 (điều chỉnh: shadcn Table đã có overflow, nên là kiểm tra + vá).
- §6 nghiệm thu → Task 8 (+ gate build/lint rải trong các task).
- §7 ngoài phạm vi → tôn trọng: không IndexedDB/push/card/background-sync.

**Placeholder scan:** không có TBD/TODO; mọi step có lệnh hoặc code cụ thể. Task 7 Step 3 có điều kiện ("chỉ khi vỡ") nhưng đã nêu rõ hành động cụ thể.

**Type consistency:** `OfflineIndicator` (Task 6) khớp import ở site-header; `/~offline` khớp giữa Task 2 (tạo) và Task 3 (fallback matcher); tên icon file khớp giữa Task 4 (sinh) và Task 5 (manifest/apple). `withSerwistInit` swSrc `app/sw.ts` khớp file Task 3.
